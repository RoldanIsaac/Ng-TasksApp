export enum TaskStatus {
  PENDING = 'PENDING',
  COMPLETED = 'COMPLETED',
}

export enum TaskEndpoints {
  getAll = 'tasks',
  getOne = 'tasks',
  create = 'tasks',
  update = 'tasks',
  delete = 'tasks',
}
